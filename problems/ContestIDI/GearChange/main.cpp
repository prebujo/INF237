#include <iostream>

using namespace std;

int main() {
    int N, M, P;
    cin >> N;
    cin >> M;
    cin >> P;
    int C[N];
    int D[M];

    for (int i = 0; i < N; ++i) {
        cin >> C[i];
    }

    for (int j = 0; j < M; ++j) {
        cin >> D[j];
    }


}